
  # Typography tool

  This is a code bundle for Typography tool. The original project is available at https://www.figma.com/design/lIZ786AWnaiLfklnGN22by/Typography-tool.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  